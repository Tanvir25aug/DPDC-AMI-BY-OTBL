═══════════════════════════════════════════════════════════
  DPDC AMI - QUICK DEPLOY TO PRODUCTION (172.18.42.200)
  Fix 504 Gateway Timeout - 5 Minute Timeout Update
═══════════════════════════════════════════════════════════

📋 AUTOMATED DEPLOYMENT (RECOMMENDED - 2 MINUTES)
═══════════════════════════════════════════════════════════

1. SSH to server:
   ssh oculin@172.18.42.200

2. Go to project directory:
   cd /home/oculin/DPDC-AMI-BY-OTBL

3. Pull latest code:
   sudo -u oculin git pull origin main

4. Run deployment script:
   sudo bash DEPLOY_NGINX_TIMEOUT_FIX.sh

5. Done! Test the application.

═══════════════════════════════════════════════════════════

📋 MANUAL DEPLOYMENT (5 MINUTES)
═══════════════════════════════════════════════════════════

1. SSH to server:
   ssh oculin@172.18.42.200

2. Backup nginx config:
   sudo cp /etc/nginx/sites-available/dpdc-ami /etc/nginx/sites-available/dpdc-ami.backup

3. Pull latest code:
   cd /home/oculin/DPDC-AMI-BY-OTBL
   sudo -u oculin git pull origin main

4. Copy new nginx config:
   sudo cp nginx-dpdc-ami-updated.conf /etc/nginx/sites-available/dpdc-ami

5. Test nginx:
   sudo nginx -t

6. Restart nginx:
   sudo systemctl restart nginx

7. Restart backend:
   pm2 restart backend

8. Done! Test the application.

═══════════════════════════════════════════════════════════

✅ TESTING CHECKLIST
═══════════════════════════════════════════════════════════

□ Open: http://172.18.42.200
□ Login to application
□ Go to "NOCS Balance Summary"
□ Click on a large NOCS (10,000+ customers)
□ Verify: Page loads successfully (30-90 seconds)
□ Verify: NO 504 timeout error
□ Verify: All customer data displays

═══════════════════════════════════════════════════════════

📊 EXPECTED PERFORMANCE AFTER UPDATE
═══════════════════════════════════════════════════════════

NOCS Size    | Load Time  | Timeout?
-------------|------------|----------
1,000        | 5-10s      | ❌ No
5,000        | 20-30s     | ❌ No
10,000       | 30-60s     | ❌ No
20,000       | 60-120s    | ❌ No
50,000+      | 2-5 min    | ❌ No

═══════════════════════════════════════════════════════════

🔍 VERIFICATION COMMANDS
═══════════════════════════════════════════════════════════

Check nginx status:
  sudo systemctl status nginx

Check backend status:
  pm2 status

View nginx logs:
  sudo tail -f /var/log/nginx/dpdc-ami-error.log

View backend logs:
  pm2 logs backend --lines 50

Verify timeout settings applied:
  sudo nginx -T | grep "proxy_read_timeout"
  (Should show: proxy_read_timeout 300s;)

═══════════════════════════════════════════════════════════

⚠️ TROUBLESHOOTING
═══════════════════════════════════════════════════════════

If nginx won't start:
  1. Check syntax: sudo nginx -t
  2. View errors: sudo tail -f /var/log/nginx/error.log
  3. Restore backup:
     sudo cp /etc/nginx/sites-available/dpdc-ami.backup /etc/nginx/sites-available/dpdc-ami
     sudo systemctl restart nginx

If still getting 504:
  1. Verify nginx restarted: sudo systemctl restart nginx
  2. Verify backend restarted: pm2 restart backend
  3. Clear browser cache: Ctrl+Shift+R

═══════════════════════════════════════════════════════════

📞 SUPPORT
═══════════════════════════════════════════════════════════

Full Documentation:
  - NGINX_UPDATE_GUIDE.md (detailed steps)
  - PRODUCTION_NGINX_TIMEOUT_FIX.md (complete guide)

Files on GitHub:
  - nginx-dpdc-ami-updated.conf (nginx config)
  - DEPLOY_NGINX_TIMEOUT_FIX.sh (auto deployment)
  - backend/src/config/oracle.js (backend timeout)

═══════════════════════════════════════════════════════════
Last Updated: 2026-01-06
Status: ✅ Ready for Production
═══════════════════════════════════════════════════════════
